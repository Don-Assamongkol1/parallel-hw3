#include "a_lock.h"

bool a_lock_init(a_lock_t* a_lock) {
    return true;
}

bool a_lock_lock(a_lock_t* a_lock) {
    return true;
}

bool a_lock_unlock(a_lock_t* a_lock) {
    return true;
}

bool a_lock_destroy(a_lock_t* a_lock) {
    return true;
}